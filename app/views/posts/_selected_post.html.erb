<div class="col col-md-6">
    <div>
        <h5>Post #<%= @post.id%> by <span><%=@user.name%></span></h5>
    </div>
    <div class="p-2 mt-2 d-flex justify-content-end w-75">
        <div class="d-flex justify-content-end w-100"> <span class="badge bg-success p-1">Comments: <%= @post.comments_counter%></span>  <span class="badge bg-dark p-1">Likes: <%= @post.likes_counter%></span></span> </div>
    </div>
    <br/>
    <p><%= @post.text%></p>
    <div class="comments">
        <% @post.latest_comments.each do |comment| %>
           <h5 class="h5"><%= comment.author.name%>:</h5> <span><%= comment.text%></span>
        <%end%>
    </div>
</div>